<%
  const options = {
    year: 'numeric', month: 'numeric', day: 'numeric',
    hour: 'numeric', minute: 'numeric', second: 'numeric',
    hour12: false,
    timeZone: 'America/Sao_Paulo'
  };
%>
<tr>
  <td><%= consumption.suitIdentifier %></td>
  <td><%= consumption.pages %></td>
  <td><%= consumption.pageCount %></td>
  <td><%= new Intl.DateTimeFormat('pt-BR', options).format(consumption.date) %></td>
</tr>